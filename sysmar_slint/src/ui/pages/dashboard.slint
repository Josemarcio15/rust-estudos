import { GraficoComponent } from "components/grafico.slint";
import { Legendas } from "components/legenda.slint";
export component DashboardPage inherits Rectangle {
    in property <int> total_clientes;
    in property <int> clientes_em_dia;
    in property <int> clientes_atrasados;
    in property <int> plano_diario;
    in property <int> plano_mensal;
    in property <int> plano_trimestral;
    in property <int> plano_semestral;
    in property <int> plano_anual;

    in property <int> percentual_clientes_em_dia;
    in property <int> percentual_clientes_atrasados;
    in property <int> percentual_plano_diario;
    in property <int> percentual_plano_mensal;
    in property <int> percentual_plano_trimestral;
    in property <int> percentual_plano_semestral;
    in property <int> percentual_plano_anual;

    callback loaded();
    init => {
        loaded();
    }

    background: #c0b6b65f;
    // width: root.width * 0.8;
    // height: root.height * 0.8;

    // ==============================================================================//
    //                                       Legendas grafico                        //
    // ==============================================================================//
    VerticalLayout {
        alignment: start;

        Legendas {
            color: #1300bb;
            legenda: "Total Alunos: ";
            dados_legenda: total_clientes;
        }

        Legendas {
            color: #7600bb;
            legenda: "Em dias: ";
            dados_legenda: total_clientes;
        }

        Legendas {
            color: #00bbbb;
            legenda: "Debitos: ";
            dados_legenda: total_clientes;
        }

        Legendas {
            color: #bb0033;
            legenda: "Diarias: ";
            dados_legenda: total_clientes;
        }

        Legendas {
            color: #60cc62;
            legenda: "Plano mensal: ";
            dados_legenda: total_clientes;
        }

        Legendas {
            color: #ffd000;
            legenda: "Plano trimestral: ";
            dados_legenda: total_clientes;
        }

        Legendas {
            color: #4c00da;
            legenda: "Plano semestral: ";
            dados_legenda: total_clientes;
        }
    }

    Rectangle {
        drop-shadow-blur: 10px;
        drop-shadow-offset-x: 1px;
        drop-shadow-offset-y: 1px;
        drop-shadow-color: rgb(129, 146, 247);
        border-radius: 10px;
        width: 50%;
        height: 50%;
        background: #d1042d;
        clip: true;
        Image {
            source: @image-url("../../assets/image1.jpg");
            width: 100%;
            height: 100%;
        }

        Rectangle {
            width: parent.width;
            height: parent.height;
            background: rgba(54, 123, 214, 0.85);
        }
            // ==============================================================================//
            //                                       Grafico                                 //
            // ==============================================================================//+
         HorizontalLayout {
            spacing: 40px;
            alignment: center;

            GraficoComponent {
                cor_base: #1300bb;
                cor_primaria: #1310bb6a;
                cor_secundaria: #1320bb2f;
                cor_terciaria: #1330bb6a;
                altura: 90;
            }

            GraficoComponent {
                cor_base: #7600bb;
                cor_primaria: #7610bb6a;
                cor_secundaria: #7620bb2f;
                cor_terciaria: #7630bb6a;
                altura: 90;
            }

            GraficoComponent {
                cor_base: #00bbbb;
                cor_primaria: #00bcbb6a;
                cor_secundaria: #00bdbb2f;
                cor_terciaria: #00bebb6a;
                altura: 90;
            }

            GraficoComponent {
                cor_base: #bb0033;
                cor_primaria: #bb10596a;
                cor_secundaria: #bb20972f;
                cor_terciaria: #bb30336a;
                altura: 90;
            }

            GraficoComponent {
                cor_base: #60cc62;
                cor_primaria: #65dd626a;
                cor_secundaria: #70de622f;
                cor_terciaria: #75df626a;
                altura: 10;
            }

            GraficoComponent {
                cor_base: #ffd000;
                cor_primaria: #ffe000c8;
                cor_secundaria: #fff0006f;
                cor_terciaria: #fff0009b;
                altura: 10;
            }

            GraficoComponent {
                cor_base: #4c00da;
                cor_primaria: #4c11da6a;
                cor_secundaria: #4c22da2f;
                cor_terciaria: #4c33da6a;
                altura: 10;
            }
        }
    }
}
