import {
    VerticalBox,
    HorizontalBox,
    Button,
} from "std-widgets.slint";

export component PrincipalWindow inherits Window {
    width: 800px;
    height: 500px;
    title: "Sistema";

    VerticalBox {
        spacing: 0px;

        Rectangle {
            height: 48px;
            background: #2c3e50;

            HorizontalBox {
                padding: 12px;
                spacing: 16px;

                Text {
                    text: "Sistema";
                    color: #ffffff;
                    font-size: 18px;
                }

                Rectangle {
                    width: 1px;
                }

                Button {
                    text: "Sair";
                }
            }
        }

        HorizontalBox {
            spacing: 0px;

            Rectangle {
                width: 200px;
                background: #ecf0f1;

                VerticalBox {
                    padding: 12px;
                    spacing: 8px;

                    Button {
                        text: "Dashboard";
                    }

                    Button {
                        text: "Arquivos";
                    }

                    Button {
                        text: "Configurações"; }
                   
                }
            }

            Rectangle {
                background: #ffffff;

                VerticalBox {
                    padding: 24px;
                    spacing: 16px;

                    Text {
                        text: "Bem-vindo ao sistema";
                        font-size: 22px;
                    }

                    Text {
                        text: "Selecione uma opção no menu lateral.";
                    }
                }
            }
        }
    }
}
